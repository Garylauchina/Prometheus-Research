## Prometheus v4.0 - 最终架构设计

**制定日期**: 2025-12-01  
**版本**: v4.0.0-dev  
**状态**: 核心代码已完成

---

## 📐 核心设计原则

### **三层架构（简洁、清晰、高效）**

```
┌─────────────────────────────────────────┐
│         Mastermind（主脑）               │
│  职责：战略决策                          │
│  ┌─────────────────────────┐           │
│  │ - LLM Oracle            │           │
│  │ - 全局策略              │           │
│  │ - 参数设置              │           │
│  └─────────────────────────┘           │
└─────────────────┬───────────────────────┘
                  │
                  ↓ 发布战略公告
        ┌─────────────────────┐
        │   公告板系统（三层）  │
        │  - 战略公告板        │
        │  - 市场公告板        │
        │  - 系统公告板        │
        └─────────────────────┘
                  ↑ 发布市场&系统公告
                  │
┌─────────────────┴───────────────────────┐
│        Supervisor（监督者）⭐            │
│  职责：监督市场 + Agent + 风险            │
│  ┌─────────────────────────┐           │
│  │ 市场分析模块：           │           │
│  │ - 计算技术指标 ⭐        │           │
│  │ - 分析市场状态 ⭐        │           │
│  │ - 判断市场难度 ⭐        │           │
│  ├─────────────────────────┤           │
│  │ Agent监控模块：          │           │
│  │ - 健康监控              │           │
│  │ - 权限管理              │           │
│  │ - 奖章颁发              │           │
│  │ - 英灵殿审核            │           │
│  ├─────────────────────────┤           │
│  │ 综合分析模块：           │           │
│  │ - 环境压力 ⭐           │           │
│  │ - 风险警告              │           │
│  │ - 群体统计              │           │
│  └─────────────────────────┘           │
└─────────────────┬───────────────────────┘
                  │
                  ↓ 只读公告板
┌─────────────────────────────────────────┐
│           Agent Population               │
│  ┌─────┐ ┌─────┐       ┌─────┐         │
│  │Ag001│ │Ag002│  ...  │Ag100│         │
│  └─────┘ └─────┘       └─────┘         │
└─────────────────────────────────────────┘
```

---

## 🎯 关键设计决策

### **1. 市场分析整合到Supervisor**

**决策**: 取消独立的`MarketAnalyzer`组件，整合到`Supervisor`

**理由**:
- ✅ Supervisor职责本来就包括"监督市场"
- ✅ 减少组件数量，避免过度设计
- ✅ 数据流动更自然（内部使用，无需传递）
- ✅ 性能更好（减少序列化开销）

**实现**:
```python
class Supervisor:
    def __init__(self):
        # 市场分析模块（内部）
        self.indicator_calculator = IndicatorCalculator()
        self.market_state_analyzer = MarketStateAnalyzer()
    
    def comprehensive_monitoring(self, market_data):
        # 1. 市场分析
        self.analyze_market_and_publish(market_data)
        
        # 2. Agent监控
        self._monitor_and_update_agents()
        
        # 3. 环境分析
        self.publish_environment_info()
```

---

### **2. 公告板三层架构**

**决策**: 简化为三层，严格权限控制

**层级**:
1. **战略公告板** (`strategic`) - 只有Mastermind可发布
2. **市场公告板** (`market`) - 只有Supervisor可发布
3. **系统公告板** (`system`) - 只有Supervisor可发布

**权限**:
```python
class BulletinBoardPermissions:
    TIER_PERMISSIONS = {
        'Mastermind': [BulletinTier.STRATEGIC],
        'Supervisor': [BulletinTier.MARKET, BulletinTier.SYSTEM]
    }
    # Agent只能读，不能发布
```

**优势**:
- ✅ 信息质量高（只有权威来源）
- ✅ 无噪音（无Agent互相干扰）
- ✅ 管理简单（不需要审核机制）
- ✅ 与三层架构完美对齐

---

### **3. 技术指标系统**

**决策**: 8大核心指标 + 指标工具箱策略

**8大核心指标**:
1. **ADX** (Average Directional Index) - 趋势强度
2. **ATR** (Average True Range) - 波动率
3. **RSI** (Relative Strength Index) - 相对强弱
4. **Stochastic** - 随机指标
5. **CCI** (Commodity Channel Index) - 商品通道
6. **EMA** (Exponential Moving Average) - 指数移动平均
7. **Bollinger Bands** - 布林带
8. **OBV** (On-Balance Volume) - 能量潮

**指标工具箱策略**:
- Supervisor统一计算一次（效率）
- 发布到公告板（市场层）
- Agent根据基因决定使用哪些指标
- Agent自主设置指标权重和参数

**优势**:
- ✅ 避免重复计算（性能优化）
- ✅ 保持Agent多样性（不同基因选择不同指标）
- ✅ 简化Agent设计（只需解读，不需计算）

---

### **4. 信息架构优化**

**决策**: 混合分层架构

**设计**:
```
Supervisor（集中计算）
    ↓
计算技术指标 + 市场状态（一次）
    ↓
发布到公告板
    ↓
Agent（分散解读）
    ↓
根据基因和性格解读信息
    ↓
做出不同的交易决策
```

**优势**:
- ✅ **集中计算**: 避免100个Agent重复计算指标（效率）
- ✅ **分散解读**: 不同Agent对同一信息有不同理解（多样性）
- ✅ **简化Agent**: Agent专注交易逻辑，不关心指标计算细节

---

## 📂 核心代码文件

### **已完成**

| 文件 | 功能 | 状态 |
|------|------|------|
| `prometheus/core/indicator_calculator.py` | 技术指标计算器 | ✅ 完成 |
| `prometheus/core/market_state_analyzer.py` | 市场状态分析器 | ✅ 完成 |
| `prometheus/core/supervisor.py` | 监督者（整合市场分析） | ✅ 完成 |
| `prometheus/core/bulletin_board_v4.py` | 公告板v4（三层架构） | ✅ 完成 |
| `prometheus/core/__init__.py` | 模块导出 | ✅ 完成 |
| `examples/v4_complete_demo.py` | 完整示例 | ✅ 完成 |

### **待完善**

| 文件 | 功能 | 状态 |
|------|------|------|
| `prometheus/core/agent_v4.py` | Agent（整合所有系统） | 🔄 待更新 |
| `prometheus/core/mastermind.py` | Mastermind（LLM主导） | 🔄 待更新 |

---

## 🔧 核心组件说明

### **IndicatorCalculator（技术指标计算器）**

```python
class IndicatorCalculator:
    """
    计算8大技术指标
    
    输入: 市场OHLCV数据
    输出: TechnicalIndicators（结构化数据）
    """
    
    def calculate_all(self, market_data) -> TechnicalIndicators:
        return TechnicalIndicators(
            trend={...},      # ADX, EMA等
            momentum={...},   # RSI, Stochastic, CCI
            volatility={...}, # ATR, Bollinger Bands
            volume={...},     # OBV
            price={...}       # 当前价、高低点
        )
```

### **MarketStateAnalyzer（市场状态分析器）**

```python
class MarketStateAnalyzer:
    """
    分析市场状态
    
    输入: TechnicalIndicators
    输出: MarketState（趋势、动量、波动率、难度、机会）
    """
    
    def analyze(self, indicators) -> MarketState:
        return MarketState(
            trend=TrendState.STRONG_UPTREND,  # 趋势状态
            momentum=MomentumState.NEUTRAL,   # 动量状态
            volatility=VolatilityState.NORMAL, # 波动状态
            market_difficulty=0.45,            # 市场难度
            opportunity_score=0.65,            # 机会评分
            recommendation="..."               # 建议
        )
```

### **Supervisor（监督者）**

```python
class Supervisor:
    """
    监督者（完整版）
    
    职责：
    1. 市场分析 → 发布市场公告
    2. Agent监控 → 健康+权限+奖章
    3. 环境分析 → 环境压力+风险警告
    """
    
    def comprehensive_monitoring(self, market_data):
        """一站式监控"""
        # 1. 市场分析
        indicators = self.indicator_calculator.calculate_all(market_data)
        market_state = self.market_state_analyzer.analyze(indicators)
        self.bulletin_board.post('market', ..., publisher='Supervisor')
        
        # 2. Agent监控
        for agent in self.agents:
            health = self.evaluate_agent(agent)
            self.update_permissions(agent)
            self.award_medals(agent)
        
        # 3. 环境分析
        pressure = self.calculate_environment_pressure()
        self.bulletin_board.post('system', ..., publisher='Supervisor')
        
        # 4. 风险警告
        if pressure > 0.7:
            self.issue_risk_warning()
```

### **BulletinBoardV4（公告板v4）**

```python
class BulletinBoardV4:
    """
    公告板v4（简化版）
    
    特点：
    - 三层架构（strategic/market/system）
    - 严格权限（Mastermind/Supervisor可发布）
    - Agent只读
    """
    
    def post(self, tier, title, content, publisher):
        """发布公告（权限检查）"""
        if not self.can_publish(publisher, tier):
            raise PermissionError
        ...
    
    def read(self, agent_id, tier=None, limit=10):
        """读取公告（Agent调用）"""
        ...
```

---

## 🚀 使用示例

### **完整工作流程**

```python
# 1. 初始化系统
bulletin_board = BulletinBoardV4()
supervisor = Supervisor(bulletin_board=bulletin_board)
mastermind = Mastermind(bulletin_board=bulletin_board)

# 2. Mastermind发布战略
mastermind.announce_strategy(
    strategy_type='conservative',
    parameters={'max_leverage': 2}
)

# 3. Supervisor综合监控
supervisor.comprehensive_monitoring(market_data)

# 4. Agent读取公告
bulletins = bulletin_board.read(agent_id, limit=5)

# 5. Agent做出决策（基于公告 + 基因 + 性格）
agent.make_trading_decision(bulletins, market_data)
```

---

## ✅ 设计优势

### **1. 简洁性**

- 三层架构（Mastermind/Supervisor/Agent）
- 三个公告板层级
- 8个核心指标
- 清晰的权限模型

### **2. 高效性**

- **集中计算**: 技术指标只计算一次
- **内部数据流**: Supervisor内部数据无需序列化
- **批处理**: `comprehensive_monitoring`一次完成所有监控

### **3. 多样性**

- **分散解读**: 不同Agent对同一指标有不同理解
- **基因驱动**: Agent基因决定指标权重和参数
- **性格影响**: 性格影响信息接受度

### **4. 可扩展性**

- 易于添加新指标（在IndicatorCalculator中）
- 易于添加新市场状态（在MarketStateAnalyzer中）
- 易于添加新公告类型（在BulletinBoard中）

---

## 📋 下一步工作

### **代码完善**

1. ✅ ~~创建IndicatorCalculator~~
2. ✅ ~~创建MarketStateAnalyzer~~
3. ✅ ~~更新Supervisor整合市场分析~~
4. ✅ ~~简化BulletinBoard为三层架构~~
5. ✅ ~~更新__init__.py导出模块~~
6. ✅ ~~创建完整示例程序~~
7. 🔄 更新AgentV4整合所有系统
8. 🔄 更新Mastermind为LLM主导模式

### **测试和优化**

1. 单元测试（各组件）
2. 集成测试（三层架构联动）
3. 性能测试（指标计算效率）
4. 回测系统（历史数据验证）

### **文档完善**

1. ✅ ~~最终架构文档~~
2. API文档（各组件接口）
3. 开发指南（如何扩展）
4. 部署指南（如何运行）

---

## 🎉 总结

**Prometheus v4.0** 采用了**简洁、高效、多样**的三层架构设计：

1. **Mastermind（主脑）**: 战略决策，LLM驱动
2. **Supervisor（监督者）**: 市场分析 + Agent监控 + 风险管理
3. **Agent（智能体）**: 自主交易，基因驱动

**核心创新**:
- ✨ **市场分析整合**: 整合到Supervisor，避免组件爆炸
- ✨ **公告板简化**: 三层架构，严格权限，信息质量高
- ✨ **信息架构优化**: 集中计算 + 分散解读，效率与多样性兼得

**设计哲学**:
> "Less is More" - 用最少的组件，实现最强的功能

---

**文档版本**: v1.0  
**最后更新**: 2025-12-01  
**作者**: Prometheus Team

