# v7.0优化想法清单

## 💡 待实现的想法

### 1. 寿命计数器机制（2025-12-10提出）

#### 核心想法

给每个Agent增加一个`lifespan_counter`计数器，每cycle递减，倒数到0自然死亡。

#### 优点

```
✅ 精确控制：基于cycle而不是generation
✅ 独立计数：每个Agent自己的寿命
✅ 封装好：Agent内部管理
✅ 不冲突：与5奖章机制独立
✅ 灵活：可以随机初始寿命（100-200）
✅ 为种群调度提供想象空间
```

#### 实现方式

```python
class MetaGenome:
    # ... 现有字段
    lifespan_counter: int = 150  # 初始寿命（cycles）
    max_lifespan: int = 150      # 最大寿命（用于重置）

# 每个cycle递减
def age_agent(agent):
    agent.meta_genome.lifespan_counter -= 1

# 死亡检查顺序
def check_and_retire_agents():
    for agent in agents:
        if agent.awards >= 5:
            retire_agent(agent, reason='hero')  # 优先：光荣退休
        elif agent.lifespan_counter <= 0:
            terminate_agent(agent, reason=AGE_LIMIT)  # 其次：寿命耗尽
        elif performance_too_poor:
            terminate_agent(agent, reason=POOR_PERFORMANCE)  # 最后：性能淘汰
```

#### v7.0应用场景

```python
# 不同生态位配置不同寿命
NICHE_LIFESPAN_CONFIG = {
    'trend_follower': 300,      # 趋势跟随：中等寿命
    'mean_reversion': 200,      # 均值回归：较短寿命
    'bull_holder': 500,         # 牛市持仓：长寿命
    'bear_shorter': 500,        # 熊市做空：长寿命
    'scalper': 100,             # 短线交易：极短寿命
    'arbitrageur': 150,         # 套利交易：短寿命
    'contrarian': 200,          # 逆向交易：较短寿命
    'profit_taker': 250,        # 止盈专家：中等寿命
    'risk_manager': 400,        # 风险管理：长寿命
    'momentum_trader': 180,     # 动量交易：较短寿命
}

# Prophet根据市场环境动态调整寿命
def adjust_lifespan_by_market(agent, market_type):
    if market_type == 'bull':
        # 牛市：延长多头寿命，缩短空头寿命
        if agent.niche in ['bull_holder', 'trend_follower']:
            agent.meta_genome.lifespan_counter += 50
        elif agent.niche == 'bear_shorter':
            agent.meta_genome.lifespan_counter -= 50
    
    elif market_type == 'bear':
        # 熊市：延长空头寿命，缩短多头寿命
        if agent.niche == 'bear_shorter':
            agent.meta_genome.lifespan_counter += 50
        elif agent.niche in ['bull_holder', 'trend_follower']:
            agent.meta_genome.lifespan_counter -= 50
```

#### 与v6.0现有机制对比

```
v6.0机制（10代死亡）:
  - agent_age = self.generation - agent.birth_generation
  - 10代 = 10 * evolution_interval = 300 cycles
  - 基于generation计数
  - 所有Agent统一寿命

v7.0机制（寿命计数器）:
  - lifespan_counter每cycle递减
  - 不同生态位不同寿命（100-500 cycles）
  - 基于cycle计数
  - 更精确、更灵活

等价设置：
  - lifespan_counter = 300
  - 等价于v6.0的10代机制
```

#### 优先级

```
Phase 3：高级特性（选做）
  - 优先级：低
  - 前提：Phase 1+2完成且验证有效
  - 目的：锦上添花，不是必需
```

#### 为什么v6.0不实现？

```
理由：
  1. v6.0已完成且验证有效（24,412个基因）
  2. 现有10代机制已足够
  3. 避免v6.0反复改动（极简主义）
  4. 递减收益（提升可能<5%）
  5. 重新训练成本（10分钟 x 3市场）
  6. 聚焦v7.0开发

决策：
  ✅ 封存此想法
  ✅ 留给v7.0 Phase 3实现
  ✅ 保持v6.0极简稳定
```

---

## 🎯 v7.0责任清单

### Phase 1：核心架构（1个月）⭐ 必须

```
目标：最小可行产品（MVP）

实现：
  1. 多生态位架构（10大生态位）
  2. Prophet基础调度（WorldSignature + 资金分配）
  3. 生态位竞争（独立排名/淘汰/繁殖）
  4. 召回v6.0基因（按生态位匹配）

验证：
  - Mock环境测试
  - 方向熵>0.5
  - 不会方向垄断
```

### Phase 2：Prophet智能化（1个月）⭐ 重要

```
目标：Prophet智能调度

实现：
  1. 动态资金分配（根据市场环境）
  2. Immigration战略调用（v6.0封存的）
  3. 方向熵监控（实时监控+触发维护）
  4. 传奇Agent召回（5奖章英雄）

验证：
  - 不同市场环境的资金分配
  - Immigration触发逻辑
  - 方向熵维持>0.5
```

### Phase 3：高级特性（选做）⚠️ 可选

```
目标：锦上添花

实现：
  1. 🆕 寿命计数器机制（本文档第1项）
  2. 🆕 传奇Agent复活（主动召回+奖励）
  3. 🆕 生态位动态调整（增删生态位）

优先级：
  - 低优先级
  - 只有Phase 1+2完成且验证有效后才做
  - 避免过度设计
```

---

## 💰 不忘初心，方得始终

```
v7.0的唯一目标：BTC市场盈利！

判断标准：
  ✅ 夏普比率 > 1.5
  ✅ 最大回撤 < 30%
  ✅ 长期跑赢BTC

重要提醒：
  ❌ 如果Phase 1+2无法盈利
  ❌ Phase 3再多功能也没用
  ❌ 所有的理想都是空想

结论：
  💰 盈利是检验真理的唯一标准！
  💰 功能不是越多越好，而是有效就好！
  💰 不忘初心，方得始终！
```

---

*文档创建时间：2025-12-10*  
*v6.0-Stage1完成后的展望*  
*所有未来愿景的前提：v7.0在BTC市场实现盈利！*

