# Evolution System - 完整进化系统文档

## 📚 目录

1. [系统概述](#系统概述)
2. [核心组件](#核心组件)
3. [资金循环系统](#资金循环系统)
4. [环境压力系统](#环境压力系统)
5. [繁殖死亡机制](#繁殖死亡机制)
6. [使用指南](#使用指南)
7. [配置参数](#配置参数)

---

## 系统概述

Evolution System是Prometheus v30的核心进化引擎，实现了完整的Agent自适应进化机制。

### 核心理念

```
模拟自然进化规律：
🌟 繁荣期 → 快速扩张（低门槛，高资助）
⚖️ 平衡期 → 正常竞争（标准机制）
🔥 危机期 → 优胜劣汰（高门槛，严格淘汰）
```

### 系统架构

```
┌─────────────────────────────────────────────────────┐
│                  Evolution System                    │
│                                                      │
│  ┌──────────────┐    ┌───────────────┐             │
│  │ Capital Pool │◄───┤ Environmental │             │
│  │    资金池     │    │   Pressure    │             │
│  │              │    │   环境压力     │             │
│  └──────┬───────┘    └───────┬───────┘             │
│         │                    │                      │
│         │ ┌──────────────────┘                      │
│         ↓ ↓                                         │
│  ┌─────────────────────┐                            │
│  │   Agent Lifecycle   │                            │
│  │    出生→成长→死亡    │                            │
│  │         ↓           │                            │
│  │    繁殖/淘汰机制     │                            │
│  └─────────────────────┘                            │
└──────────────────────────────────────────────────────┘
```

---

## 核心组件

### 1. EnhancedCapitalPool - 增强资金池

**文件**: `evolution/capital_pool.py`

**职责**:
- 资金分配追踪
- 死亡资金回收
- 繁殖资金资助
- 状态实时监控

**核心方法**:
```python
pool = EnhancedCapitalPool(10000)

# 分配
pool.allocate_to_agent(2000)

# 回收
pool.recycle_from_death(1500, recovery_rate=1.0)

# 资助
pool.subsidize_reproduction(800)

# 查询
status = pool.get_status()
```

### 2. EnvironmentalPressure - 环境压力

**文件**: `evolution/environmental_pressure.py`

**职责**:
- 计算环境压力值
- 识别当前阶段
- 动态调整繁殖/死亡参数

**核心方法**:
```python
pressure = EnvironmentalPressure()

# 更新
p = pressure.update(market, agents, pool_status)

# 获取阶段
phase, name = pressure.get_phase()

# 调整配置
config = pressure.adjust_reproduction_config(base_config)
```

---

## 资金循环系统

### 设计原理

```
资金守恒定律：
初始资金 = 在用资金 + 可用资金 + 死亡损耗(0%)

完整循环：
创建 → Agent持有 → 死亡 → 100%回收 → 资金池 → 
资助 → 新Agent → 循环
```

### 资金流动图

```
                     ┌─────────────┐
                     │  资金池      │
                     │  Total:10000│
                     └──┬────────┬──┘
                        │        │
            分配80%     │        │  回收100%
                        ↓        ↑
                  ┌─────────┐   │
                  │ Agent   │   │
                  │ $8000   │   │
                  └────┬────┘   │
                       │        │
              繁殖     │        │  死亡
              ↓        ↓        ↑
        ┌─────────┐  成长  ┌─────────┐
        │子Agent  │  交易  │死亡Agent │
        │父20%+   │ ←──→  │剩余资金  │
        │池30%    │  ROI   │         │
        └─────────┘        └─────────┘
             ↓                   ↑
             └───────循环────────┘
```

### 资金分配策略

#### 创世生成
```
总资金: $10,000
├─ Agent分配: $8,000 (80%, 15个×$533)
└─ 资金池保留: $2,000 (20%)

目的: 预留资金用于繁殖资助
```

#### 繁殖资助
```
新Agent资金来源:
├─ 父代转移: 20% × 父代资金
├─ 资金池资助: 30% × 初始Agent资金
└─ 繁殖成本: 5% × 父代资金 (损耗)

示例 (父代$633初始，当前$800):
├─ 父代给: $160 (20% × $800)
├─ 资金池给: $190 (30% × $633)
├─ 父代损耗: $40 (5% × $800)
└─ 子代获得: $350 ($160 + $190)
   父代剩余: $600 ($800 - $160 - $40)
```

#### 死亡回收
```
Agent死亡时:
├─ 剩余资金: $456
├─ 回收率: 100%
└─ 回到资金池: $456

资金池更新:
├─ available_capital += $456
├─ allocated_capital -= $456
└─ recycled_capital += $456 (统计)
```

---

## 环境压力系统

### 压力计算公式

```
pressure = Σ (各因素 × 权重)

三大因素:
┌─────────────────────────────────────┐
│ 1. 市场因素 (40%)                    │
│    = (波动率×0.6 + 恐慌×0.4) × 0.4  │
│                                     │
│ 2. 种群因素 (30%)                    │
│    = ((1-ROI)×0.6 + (1-存活)×0.4)×0.3│
│                                     │
│ 3. 资金池因素 (30%)                  │
│    = U型曲线(利用率)                 │
└─────────────────────────────────────┘
```

### 压力值含义

```
压力值   阶段      特征                策略
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0.0-0.1  极繁荣   市场好+种群强+资金足  极度扩张
0.1-0.3  繁荣期   条件良好              鼓励繁殖
0.3-0.5  正常期   适度竞争              标准机制
0.5-0.7  压力期   开始困难              谨慎运作
0.7-0.9  危机期   资源紧张              优胜劣汰
0.9-1.0  极危机   生死存亡              极度淘汰
```

### 压力对繁殖的影响

| 参数 | 繁荣期(×) | 平衡期 | 危机期(×) | 说明 |
|------|-----------|--------|-----------|------|
| min_roi | 0.7 | 1.0 | 1.3 | ROI要求 |
| min_trades | -1 | 0 | +1 | 交易次数 |
| pool_subsidy | 1.5 | 1.0 | 0.5 | 资金池资助 |

**实例**:
```
基准配置: ROI>5%, 交易>=2, 资助30%

繁荣期调整:
├─ ROI要求: 5% × 0.7 = 3.5% ✅
├─ 交易次数: 2 - 1 = 1次 ✅
└─ 资助比例: 30% × 1.5 = 45% ✅

危机期调整:
├─ ROI要求: 5% × 1.3 = 6.5% ⚠️
├─ 交易次数: 2 + 1 = 3次 ⚠️
└─ 资助比例: 30% × 0.5 = 15% ⚠️
```

### 压力对死亡的影响

| 参数 | 繁荣期(×) | 平衡期 | 危机期(×) | 说明 |
|------|-----------|--------|-----------|------|
| death_roi_threshold | 1.2 | 1.0 | 0.8 | 绝对阈值 |
| parent_protection | +2 | 0 | -1 | 父代保护 |
| elite_threshold | 0.8 | 1.0 | 1.2 | 精英门槛 |

**实例**:
```
基准配置: 阈值-35%, 父代保护3周期, 精英>20%

繁荣期调整:
├─ 死亡阈值: -35% × 1.2 = -42% ✅
├─ 父代保护: 3 + 2 = 5周期 ✅
└─ 精英门槛: 20% × 0.8 = 16% ✅

危机期调整:
├─ 死亡阈值: -35% × 0.8 = -28% ⚠️
├─ 父代保护: 3 - 1 = 2周期 ⚠️
└─ 精英门槛: 20% × 1.2 = 24% ⚠️
```

---

## 繁殖死亡机制

### 繁殖机制（5条件）

```python
def can_reproduce(config):
    检查清单:
    ✅ 1. is_alive = True
    ✅ 2. ROI > min_roi (环境调整)
    ✅ 3. trade_count >= min_trades (环境调整)
    ✅ 4. age >= min_age
    ✅ 5. children_count < max_children
    
    全部满足 → 可以繁殖
```

### 死亡机制（7层判断）

```python
def should_die(config, all_agents):
    第1层: ROI < -35% (环境调整)
           → 立即死亡
    
    第2层: age < 3
           → 年龄保护，存活
    
    第3层: 繁殖后 < 3周期 (环境调整)
           → 父代保护，存活
    
    第4层: ROI > 20% (环境调整)
           → 精英特权，存活
    
    第5层: 排名后20% 且 ROI < -10%
           → 相对淘汰
    
    第6层: age > 20 且 ROI < 0
           → 长期低效淘汰
    
    第7层: 波动率 > 50% 且 ROI < 0
           → 高风险淘汰
    
    全不触发 → 存活
```

---

## 使用指南

### 基础使用

```python
from evolution import EnhancedCapitalPool, EnvironmentalPressure

# 1. 初始化系统
capital_pool = EnhancedCapitalPool(initial_capital=10000)
env_pressure = EnvironmentalPressure()

# 2. 创建初始Agent
for i in range(15):
    if capital_pool.allocate_to_agent(533):
        agent = Agent(i, initial_capital=533)
        agents.append(agent)

# 3. 每个进化周期
def evolution_cycle():
    # 3.1 更新压力
    pool_status = capital_pool.get_status()
    pressure = env_pressure.update(market, agents, pool_status)
    
    # 3.2 获取调整后的配置
    repro_config = env_pressure.adjust_reproduction_config(base_repro)
    death_config = env_pressure.adjust_death_config(base_death)
    
    # 3.3 执行死亡
    for agent in agents:
        if agent.should_die(death_config, agents):
            recycled = agent.die(capital_pool)
            print(f"回收${recycled}")
    
    # 3.4 执行繁殖
    for agent in agents:
        if agent.can_reproduce(repro_config):
            child = agent.reproduce(id, repro_config, capital_pool)
            agents.append(child)
```

### 集成到现有系统

```python
# 在TradingSystem中集成
class TradingSystem:
    def __init__(self, config):
        # 使用Evolution模块
        self.capital_pool = EnhancedCapitalPool(
            config['initial_capital']
        )
        self.environmental_pressure = EnvironmentalPressure()
        
    def run_evolution_cycle(self):
        # 自动化进化流程
        ...
```

---

## 配置参数

### 繁殖参数

```python
reproduction_config = {
    # 基础条件
    'min_roi': 0.05,              # 最低ROI要求（5%）
    'min_trades': 2,              # 最少交易次数
    'min_age': 3,                 # 最少年龄
    'max_children': 2,            # 最多子代数
    
    # 资金配置
    'parent_transfer_ratio': 0.20,   # 父代转移比例（20%）
    'pool_subsidy_ratio': 0.30,      # 资金池资助比例（30%初始资金）
    'reproduction_cost': 0.05,       # 繁殖成本（5%）
    
    # 基因变异
    'mutation_rate': 0.15            # 变异率（15%）
}
```

### 死亡参数

```python
death_config = {
    # 绝对淘汰
    'death_roi_threshold': -0.35,       # ROI绝对阈值（-35%）
    
    # 保护机制
    'min_age_for_death': 3,             # 年龄保护期（3周期）
    'parent_protection_period': 3,      # 父代保护期（3周期）
    'elite_roi_threshold': 0.20,        # 精英特权阈值（20%）
    
    # 长期评估
    'max_age_low_performance': 20       # 长期低效年龄阈值
}
```

### 环境压力参数

```python
pressure_config = {
    # 阶段阈值
    'prosperity_threshold': 0.3,     # 繁荣期阈值
    'crisis_threshold': 0.7,         # 危机期阈值
    
    # 权重分配
    'market_weight': 0.4,            # 市场因素权重
    'population_weight': 0.3,        # 种群因素权重
    'capital_weight': 0.3,           # 资金因素权重
    
    # 平滑系数
    'smoothing_old': 0.7,            # 旧值权重
    'smoothing_new': 0.3             # 新值权重
}
```

---

## 性能指标

### 资金池指标

```python
metrics = pool.get_metrics()

{
    'utilization_rate': 0.815,        # 资金利用率 81.5%
    'recycling_efficiency': 0.234,    # 回收效率 23.4%
    'subsidy_rate': 0.123,            # 资助率 12.3%
    'capital_turnover': 0.357         # 资金周转率 35.7%
}
```

### 进化统计

```python
evolution_stats = {
    'total_births': 18,               # 总出生数
    'total_deaths': 6,                # 总死亡数
    'total_reproductions': 3,         # 繁殖次数
    'survival_rate': 0.80,            # 存活率
    'pressure_avg': 0.456             # 平均压力
}
```

---

## 最佳实践

### 1. 资金池管理

```python
# ✅ 推荐：定期检查资金池状态
if pool.get_status()['utilization'] > 0.95:
    logger.warning("资金池接近耗尽！")

# ✅ 推荐：在繁殖前检查可用资金
if pool.get_status()['available'] < requested_subsidy:
    logger.info("资金不足，跳过繁殖")
```

### 2. 压力监控

```python
# ✅ 推荐：记录压力历史用于分析
pressure_history = env_pressure.history

# ✅ 推荐：根据压力调整策略
if pressure > 0.8:
    # 极度危机，考虑特殊措施
    emergency_mode = True
```

### 3. 参数调优

```python
# ✅ 推荐：根据测试时长调整
if test_duration < 10:  # 短期测试
    config['min_roi'] *= 0.8  # 降低门槛
    config['min_trades'] = 1  # 减少要求
```

---

## 故障排查

### 问题1：繁殖率过低

```
症状: 很少或没有Agent繁殖

检查:
1. 压力是否过高？ → 降低压力权重
2. ROI要求是否过高？ → 降低min_roi
3. 交易次数是否不足？ → 降低min_trades
4. 资金池是否充足？ → 检查available_capital
```

### 问题2：种群快速灭绝

```
症状: Agent大量死亡，种群崩溃

检查:
1. 死亡阈值是否过严？ → 放宽death_roi_threshold
2. 保护期是否过短？ → 延长protection_period
3. 压力是否极高？ → 检查压力来源
4. 市场是否极端？ → 调整市场因素权重
```

### 问题3：资金池枯竭

```
症状: available_capital → 0

检查:
1. 繁殖资助是否过高？ → 降低pool_subsidy_ratio
2. 回收是否正常？ → 检查recycle_from_death调用
3. 初始分配是否过多？ → 降低初始分配比例
```

---

## 版本历史

### v2.0.0 (2025-12-01)
- ✨ 新增环境压力系统
- ✨ 新增资金循环机制
- ✨ 自适应繁殖/死亡参数
- ✨ 完整文档和示例

### v1.0.0
- 🎉 基础Agent进化系统

---

## 相关文档

- [Evolution模块README](../evolution/README.md)
- [资金池API文档](./CAPITAL_POOL_API.md)
- [压力系统详解](./PRESSURE_SYSTEM.md)
- [使用示例](../examples/simple_evolution_demo.py)

---

**Author**: Prometheus Evolution Team  
**License**: Proprietary  
**Last Updated**: 2025-12-01

