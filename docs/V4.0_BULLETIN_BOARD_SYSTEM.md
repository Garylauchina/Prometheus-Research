# Prometheus v4.0 - 公告板系统

## 核心理念

**公告板 = 环境信息，而非指令**

类似自然界的信息素、气味、声音，Agent可以感知但不强制响应。

```
公告板提供信息 → Agent自主判断 → Agent自主决策
```

**关键原则**：
- ✅ 提供信息
- ✅ Agent自主选择是否关注
- ❌ 不是命令
- ❌ 不强制执行

---

## 一、公告板分层设计

### 第一层：全局公告板（主脑发布）

**战略级信息**

```python
class GlobalBulletinBoard:
    """主脑发布的全局信息"""
    
    def __init__(self):
        self.bulletins = []
    
    def post_by_mastermind(self, message):
        """
        主脑发布战略信息
        
        类型：
        1. 市场环境评估
        2. 风险等级调整
        3. 系统策略方向
        """
        bulletin = {
            'type': 'MASTERMIND_STRATEGIC',
            'timestamp': datetime.now(),
            'content': message,
            'priority': 'HIGH',
            'source': 'Mastermind',
            'expires': datetime.now() + timedelta(days=7)
        }
        
        self.bulletins.append(bulletin)
        
        # 示例
        """
        【主脑战略公告】
        当前市场状态：高波动
        建议风险等级：保守
        环境压力：提高到 1.5
        
        注：这只是建议，Agent可自主判断
        """
```

**示例公告**：
```
📢 主脑公告 [2025-12-01 10:00]
━━━━━━━━━━━━━━━━━━━━━━
市场环境：熊市转震荡
风险等级：中等
建议策略：防守为主，机会为辅

这是环境评估，非强制指令。
━━━━━━━━━━━━━━━━━━━━━━
```

---

### 第二层：市场公告板（外部信息）

**重要事件和新闻**

```python
class MarketBulletinBoard:
    """外部重要信息"""
    
    def post_market_event(self, event):
        """
        发布市场重要事件
        
        来源：
        1. 新闻API
        2. 监管公告
        3. 技术更新
        4. 重大事件
        """
        bulletin = {
            'type': 'MARKET_EVENT',
            'timestamp': datetime.now(),
            'content': event,
            'impact_level': self.assess_impact(event),
            'tags': self.extract_tags(event)
        }
        
        self.bulletins.append(bulletin)
```

**示例公告**：
```
🌍 市场事件 [2025-12-01 09:30]
━━━━━━━━━━━━━━━━━━━━━━
事件：美联储加息50个基点
影响：高
相关：BTC, ETH, 全市场

预期影响：短期波动加剧
━━━━━━━━━━━━━━━━━━━━━━

🔔 监管公告 [2025-12-01 08:00]
━━━━━━━━━━━━━━━━━━━━━━
事件：OKX调整手续费
影响：中
相关：所有交易对

费率变化：Maker 0.08% → 0.10%
━━━━━━━━━━━━━━━━━━━━━━
```

---

### 第三层：系统公告板（监督者发布）

**系统状态和风险警告**

```python
class SystemBulletinBoard:
    """监督者发布的系统信息"""
    
    def post_risk_warning(self, warning):
        """
        发布风险警告
        
        类型：
        1. 群体风险
        2. 市场异常
        3. 涅槃通知
        4. 基因库状态
        """
        bulletin = {
            'type': 'RISK_WARNING',
            'timestamp': datetime.now(),
            'content': warning,
            'severity': self.calculate_severity(warning)
        }
        
        self.bulletins.append(bulletin)
```

**示例公告**：
```
⚠️ 风险警告 [2025-12-01 11:00]
━━━━━━━━━━━━━━━━━━━━━━
警告：检测到大量止损集中
位置：50000 USDT 附近
风险：可能触发连锁止损

建议：注意风险管理
━━━━━━━━━━━━━━━━━━━━━━

🔥 涅槃通知 [2025-12-01 10:30]
━━━━━━━━━━━━━━━━━━━━━━
事件：市场崩盘触发涅槃
行动：复活15个Agent
策略：逆向抄底

新Agent已进入市场
━━━━━━━━━━━━━━━━━━━━━━

📊 群体统计 [2025-12-01 10:00]
━━━━━━━━━━━━━━━━━━━━━━
存活Agent：45个
平均盈利：+5.2%
策略多样性：0.72

系统运行正常
━━━━━━━━━━━━━━━━━━━━━━
```

---

### 第四层：社交公告板（Agent之间）

**Agent之间的信息共享**

```python
class SocialBulletinBoard:
    """Agent之间的信息交流"""
    
    def post_agent_signal(self, agent_id, signal):
        """
        Agent发布信号
        
        注意：
        - Agent可以选择发布或不发布
        - 其他Agent可以选择关注或忽略
        - 完全自愿
        """
        bulletin = {
            'type': 'AGENT_SIGNAL',
            'timestamp': datetime.now(),
            'agent_id': agent_id,
            'signal': signal,
            'credibility': self.calculate_credibility(agent_id)
        }
        
        self.bulletins.append(bulletin)
```

**示例公告**：
```
🤖 Agent信号 [2025-12-01 11:05]
━━━━━━━━━━━━━━━━━━━━━━
来自：Agent-045 (⭐⭐⭐⭐)
信号：识别到大额吸筹
置信度：0.85

其他Agent可参考
━━━━━━━━━━━━━━━━━━━━━━

🏅 传奇Agent [2025-12-01 10:50]
━━━━━━━━━━━━━━━━━━━━━━
Agent-023 获得【传奇】奖章
擅长：反散户策略
胜率：78%

可参考其行为模式
━━━━━━━━━━━━━━━━━━━━━━
```

---

## 二、信息类型设计

### 1. 战略信息（主脑）

```python
STRATEGIC_BULLETINS = {
    'market_assessment': {
        'regime': 'bull/bear/ranging',
        'volatility': 'low/medium/high',
        'risk_level': 1-5,
        'recommendation': 'text'
    },
    
    'policy_change': {
        'parameter': 'name',
        'old_value': ...,
        'new_value': ...,
        'reason': 'text'
    },
    
    'strategic_focus': {
        'focus_area': 'defense/offense/balanced',
        'duration': 'days',
        'context': 'text'
    }
}
```

### 2. 市场事件（外部）

```python
MARKET_EVENTS = {
    'news': {
        'title': 'text',
        'summary': 'text',
        'impact': 'high/medium/low',
        'tags': ['tag1', 'tag2']
    },
    
    'regulatory': {
        'type': 'policy/rule/restriction',
        'content': 'text',
        'effective_date': 'datetime'
    },
    
    'technical': {
        'type': 'upgrade/fork/bug',
        'affected': ['BTC', 'ETH'],
        'details': 'text'
    },
    
    'whale_activity': {
        'type': 'accumulation/distribution',
        'amount': 'large',
        'confidence': 0.8
    }
}
```

### 3. 风险警告（监督者）

```python
RISK_WARNINGS = {
    'population_risk': {
        'issue': 'mass_extinction/low_diversity',
        'severity': 'critical/high/medium',
        'action_taken': 'text'
    },
    
    'market_risk': {
        'type': 'volatility/liquidity/manipulation',
        'level': 'extreme/high/medium',
        'recommendation': 'text'
    },
    
    'system_risk': {
        'type': 'performance/stability/resource',
        'status': 'text',
        'mitigation': 'text'
    }
}
```

### 4. 社交信号（Agent）

```python
AGENT_SIGNALS = {
    'pattern_discovery': {
        'agent_id': 'xxx',
        'pattern': 'description',
        'confidence': 0.8,
        'credibility': 0.9  # 发布者信誉
    },
    
    'opportunity_alert': {
        'agent_id': 'xxx',
        'opportunity': 'description',
        'time_sensitive': True,
        'risk_level': 'medium'
    },
    
    'strategy_sharing': {
        'agent_id': 'xxx',
        'strategy_type': 'anti_retail',
        'recent_performance': 0.15,
        'sample_size': 50
    }
}
```

---

## 三、Agent如何使用公告板

### 完全自主选择

```python
class AgentV4:
    """Agent自主使用公告板"""
    
    def __init__(self):
        # Agent自己决定是否关注公告板
        self.bulletin_subscription = {
            'global': True,      # 关注主脑公告
            'market': True,      # 关注市场事件
            'system': True,      # 关注系统警告
            'social': False      # 不关注社交信号（Agent自己选择）
        }
        
        # Agent自己决定如何解读信息
        self.information_processing = {
            'trust_level': 0.7,  # 对公告板的信任度
            'filter_threshold': 0.5,  # 过滤阈值
            'reaction_speed': 0.8  # 反应速度
        }
    
    def check_bulletin_board(self):
        """
        检查公告板（可选）
        
        Agent完全自主决定：
        - 是否检查
        - 检查哪些
        - 如何解读
        - 是否采纳
        """
        if not self.wants_to_check_bulletin():
            return None
        
        # 获取相关公告
        bulletins = self.filter_bulletins(
            bulletin_board.get_recent()
        )
        
        # 自主解读
        interpreted_info = self.interpret(bulletins)
        
        # 自主决定是否采纳
        if self.should_act_on_info(interpreted_info):
            self.adjust_behavior(interpreted_info)
```

### 不同性格的Agent对公告板的态度

```python
# 独立型Agent
independent_agent = {
    'personality': {
        'independence': 0.9,
        'herd_mentality': 0.1
    },
    'bulletin_usage': {
        'check_frequency': 'rarely',
        'trust_level': 0.3,
        'reaction': 'skeptical'
    }
}

# 社交型Agent
social_agent = {
    'personality': {
        'independence': 0.3,
        'herd_mentality': 0.8
    },
    'bulletin_usage': {
        'check_frequency': 'frequently',
        'trust_level': 0.8,
        'reaction': 'quick'
    }
}

# 谨慎型Agent
cautious_agent = {
    'personality': {
        'discipline': 0.9,
        'risk_tolerance': 0.3
    },
    'bulletin_usage': {
        'check_frequency': 'regular',
        'trust_level': 0.9,
        'focus': 'risk_warnings'  # 只关注风险
    }
}
```

---

## 四、公告板实现

```python
class BulletinBoardSystem:
    """完整的公告板系统"""
    
    def __init__(self):
        # 四层公告板
        self.global_board = GlobalBulletinBoard()      # 主脑
        self.market_board = MarketBulletinBoard()      # 外部
        self.system_board = SystemBulletinBoard()      # 监督者
        self.social_board = SocialBulletinBoard()      # Agent
    
    def post(self, board_type, content, **kwargs):
        """发布公告"""
        boards = {
            'global': self.global_board,
            'market': self.market_board,
            'system': self.system_board,
            'social': self.social_board
        }
        
        board = boards.get(board_type)
        if board:
            board.post(content, **kwargs)
    
    def get_bulletins(self, agent_subscription):
        """
        根据Agent订阅获取公告
        
        Args:
            agent_subscription: Agent的订阅配置
            
        Returns:
            List[Bulletin]: 相关公告列表
        """
        bulletins = []
        
        if agent_subscription.get('global'):
            bulletins.extend(self.global_board.get_recent(hours=24))
        
        if agent_subscription.get('market'):
            bulletins.extend(self.market_board.get_recent(hours=6))
        
        if agent_subscription.get('system'):
            bulletins.extend(self.system_board.get_recent(hours=12))
        
        if agent_subscription.get('social'):
            bulletins.extend(self.social_board.get_recent(hours=1))
        
        return bulletins
    
    def cleanup_expired(self):
        """清理过期公告"""
        for board in [self.global_board, self.market_board, 
                     self.system_board, self.social_board]:
            board.remove_expired()
```

---

## 五、使用场景示例

### 场景1：重大新闻发布

```python
# 1. 外部新闻
news = {
    'title': 'SEC批准比特币ETF',
    'impact': 'high',
    'sentiment': 'positive'
}
bulletin_board.post('market', news)

# 2. 主脑评估并发布
mastermind_assessment = """
【主脑评估】
事件：ETF批准
影响：预期短期暴涨
建议：注意追高风险

保持理性，不被情绪左右
"""
bulletin_board.post('global', mastermind_assessment)

# 3. Agent自主反应
for agent in agents:
    # Agent自己决定如何处理这个信息
    bulletins = agent.check_bulletin_board()
    
    if agent.personality.optimism > 0.7:
        # 乐观Agent可能追涨
        agent.consider_buying()
    elif agent.personality.contrarian > 0.7:
        # 逆向Agent可能等回调
        agent.wait_for_pullback()
```

### 场景2：系统风险警告

```python
# 监督者检测到风险
risk = {
    'type': 'mass_extinction',
    'severity': 'high',
    'details': '过去1小时20个Agent死亡'
}
bulletin_board.post('system', risk)

# Agent自主反应
for agent in agents:
    bulletins = agent.check_bulletin_board()
    
    if agent.personality.fear_sensitivity > 0.7:
        # 敏感Agent可能减仓
        agent.reduce_position()
    elif agent.personality.aggression > 0.7:
        # 激进Agent可能看作机会
        agent.look_for_opportunity()
```

### 场景3：Agent分享发现

```python
# Agent-045发现了一个模式
agent_045.share_discovery({
    'pattern': '凌晨2-4点，某Bot有规律',
    'confidence': 0.85,
    'profitability': 0.12
})

bulletin_board.post('social', {
    'agent_id': 'Agent-045',
    'discovery': ...,
    'credibility': 0.95  # Agent-045是传奇Agent
})

# 其他Agent可能学习
for agent in agents:
    if agent.personality.learning_rate > 0.6:
        # 愿意学习的Agent可能采纳
        agent.try_new_pattern()
```

---

## 六、进化影响

### 对公告板敏感度也会进化

```python
# 基因中加入公告板相关参数
gene = {
    ...
    'bulletin_board_sensitivity': random.uniform(0, 1),
    'preferred_bulletin_types': random.choice([...]),
    'information_discount_factor': random.uniform(0.5, 1.0)
}

# 进化结果可能发现
result = {
    '完全忽视公告板的Agent': '可能更独立，适应性差',
    '过度依赖公告板的Agent': '可能失去自主性，从众',
    '适度参考公告板的Agent': '可能表现最好'
}

# 市场决定最优平衡点
```

---

## 七、实施建议

### 1. 数据源集成

```python
class ExternalDataFeeds:
    """外部数据源"""
    
    def __init__(self):
        self.news_api = NewsAPI()
        self.social_media = SocialMediaAPI()
        self.onchain_data = OnChainDataAPI()
    
    def fetch_and_post(self):
        """定期获取并发布"""
        # 新闻
        news = self.news_api.get_crypto_news()
        for item in news:
            bulletin_board.post('market', item)
        
        # 链上数据
        whale_moves = self.onchain_data.get_large_transactions()
        for move in whale_moves:
            bulletin_board.post('market', {
                'type': 'whale_activity',
                'details': move
            })
```

### 2. 信息质量控制

```python
class InformationQualityControl:
    """信息质量控制"""
    
    def validate_bulletin(self, bulletin):
        """验证公告质量"""
        # 1. 来源可信度
        if not self.is_trusted_source(bulletin.source):
            bulletin.credibility *= 0.5
        
        # 2. 内容一致性
        if self.conflicts_with_recent(bulletin):
            bulletin.flag_conflict = True
        
        # 3. 时效性
        if self.is_outdated(bulletin):
            bulletin.expired = True
```

### 3. Agent反馈机制

```python
class BulletinFeedback:
    """Agent反馈机制"""
    
    def track_bulletin_effectiveness(self, agent, bulletin):
        """
        追踪公告对Agent的影响
        
        如果Agent根据公告调整策略：
        - 记录结果
        - 更新公告的有效性评分
        - Agent学习哪些公告有用
        """
        if agent.acted_on_bulletin(bulletin):
            result = agent.get_outcome()
            
            # 更新公告评分
            bulletin.effectiveness_score += result
            
            # Agent学习
            agent.update_bulletin_trust(bulletin.type, result)
```

---

## 八、风险和注意事项

### 风险1：信息过载

```python
# 解决方案：过滤和优先级
class BulletinFilter:
    def filter_for_agent(self, agent, bulletins):
        # 1. 按优先级
        high_priority = [b for b in bulletins if b.priority == 'HIGH']
        
        # 2. 按相关性
        relevant = [b for b in high_priority if self.is_relevant(b, agent)]
        
        # 3. 限制数量
        return relevant[:10]  # 最多10条
```

### 风险2：从众效应

```python
# 解决方案：多样性保护
class DiversityProtection:
    def check_herd_behavior(self, agents):
        # 如果所有Agent都根据同一公告行动
        if self.all_acting_on_same_info(agents):
            # 增加随机性
            self.increase_exploration_rate()
            
            # 发布警告
            bulletin_board.post('system', {
                'type': 'herd_warning',
                'message': '警告：检测到从众行为'
            })
```

### 风险3：虚假信息

```python
# 解决方案：信誉系统
class CredibilitySystem:
    def rate_source(self, source):
        # 追踪信息源的历史准确性
        history = self.get_source_history(source)
        accuracy = sum(h['correct'] for h in history) / len(history)
        
        return accuracy
```

---

## 九、总结

### 公告板系统的价值

1. **信息透明**
   - Agent能感知外部环境
   - 不局限于技术指标

2. **知识共享**
   - 优秀Agent的发现可以传播
   - 加速群体学习

3. **风险预警**
   - 及时通知系统风险
   - 提高整体安全性

4. **战略引导**
   - 主脑的战略信息
   - 不强制但提供方向

### 核心原则

```
✅ 提供信息，不是命令
✅ Agent自主选择，不是强制
✅ 多样性反应，不是统一
✅ 进化决定，不是预设
```

### 与自主性的平衡

```
公告板（环境信息）
    ↓
Agent自主感知
    ↓
Agent自主解读
    ↓
Agent自主决策
    ↓
完全自主！
```

---

**公告板是"环境"的一部分，而非"控制"！** 📢

最后更新：2025年12月1日

