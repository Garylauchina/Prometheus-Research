# Trial-4 — World-Conditioned Impedance (E→M coupling) — Archived Artifacts (v0, 2026-01-09)

Scope (frozen by pre-reg):
- Dataset A: BTC 2021–2022
- Dataset B: ETH 2024-Q4
- 3 seeds × 4 ablation modes (`full/no_e/no_m/null`) = 12 runs per dataset
- `probe_attempts_per_tick=1`
- Runner enables `world_conditioned_impedance=1` and writes `world_r/world_u` into `interaction_impedance.jsonl.metrics`
- Reachability lens unchanged: `local_reachability.jsonl` generated by Trial-2 impedance proxy posthoc

Inputs:
- `run_dirs_A.txt`
- `run_dirs_B.txt`
- `PROGRAMMER_COMPLETION_REPORT.md` (operator report; includes Quant commit hash)

Verification:
- `verify_local_reachability_reports.jsonl` contains 24 JSON records from `verify_local_reachability_v0.py` (fail-closed schema checks). All must be `verdict=PASS`.

Grouped summaries (descriptive only):
- `grouped_A.json`
- `grouped_B.json`

Coupling sanity (sample-only):
- `world_u_latency_sample_stats.json` computes descriptive stats for `world_u` and `avg_ack_latency_ms` for one representative run per dataset, plus Pearson correlation `corr(world_u, avg_ack_latency_ms)` to confirm the coupling is active and auditable.

