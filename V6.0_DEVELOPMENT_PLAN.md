# 🚀 Prometheus v6.0 开发计划

**制定日期**: 2025-12-05 19:25  
**基于版本**: v5.2 (Day 3 完成 + 对手系统激活)  
**制定依据**: 50轮×2次对比测试 + 深度策略演化分析

---

## 📋 v5.2 当前状态总结

### ✅ 已完成的核心功能

#### 1. 多样性监控系统 (Day 3)
- ✅ `DiversityMonitor`: 实时监控种群多样性
  - 基因熵、策略熵、血统熵
  - 活跃家族数、多样性得分
  - 健康状态评估
- ✅ `DiversityProtector`: 多样性保护机制
  - 生态位保护
  - 稀有策略/血统保护
  - 强制多样化繁殖
  - 新基因注入
- ✅ `DiversityVisualizer`: 可视化系统
  - 多样性趋势图
  - 警报时间线
  - 多样性热力图
  - 综合仪表板

#### 2. 对手系统 (Day 3+)
- ✅ `SimpleInstitution`: 机构玩家（趋势跟随）
- ✅ `SimpleRetailer`: 散户玩家（动量追逐）
- ✅ `SimpleOpponentMarket`: 市场环境模拟
- ✅ 价格波动注入机制（自然波动±0.8%）
- ✅ 对手交易行为验证（593笔交易）

#### 3. 长期测试框架
- ✅ 50轮完整进化测试
- ✅ 数据记录和可视化
- ✅ 对比分析工具

### 📊 关键测试结果

#### 对比测试（静态 vs 动态市场）
| 指标 | 静态市场 | 动态市场 | 变化 |
|------|----------|----------|------|
| 最终种群 | 42个 | 53个 | **+26.2%** ✅ |
| 策略多样性 | 4.540 | 4.705 | **+3.7%** ✅ |
| 活跃家族 | 5.4个 | 6.6个 | **+22.6%** ✅ |
| 种群稳定性 | ±2.90 | ±2.24 | **+22.8%** ✅ |
| 资金收益 | +0.44% | +0.18% | -0.26pp ⚠️ |

### ⚠️ 发现的主要问题

#### 问题1：基因熵过低 🚨
```
当前: 0.166 (平均)
目标: 2.000
差距: -91.7%

风险: 基因池枯竭 → 应对突变能力下降
```

#### 问题2：活跃家族数偏少 🚨
```
当前: 6.6个 (平均)
目标: 10个
差距: -34%

风险: 家族集中 → 多样性不足
```

#### 问题3：多样性得分未达标 ⚠️
```
当前: 0.413 (平均)
目标: 0.500
差距: -17.4%

原因: 基因熵和家族数都偏低
```

#### 问题4：Agent未能抓住大涨机会 ⚠️
```
市场价格: +23% (大涨)
Agent资金: +0.18% (持平)

原因: 虚拟交易 + 保守策略 + 适应滞后
```

---

## 🎯 v6.0 总体目标

### 核心使命
**"让Agent在真实市场环境中展现出强大的适应能力和盈利能力"**

### 三大支柱

1. **多样性强化** 🧬
   - 提升基因熵: 0.166 → 0.500+
   - 增加活跃家族: 6.6 → 10+
   - 达标多样性得分: 0.413 → 0.500+

2. **真实市场测试** 📈
   - 引入真实历史K线数据
   - 完整的回测环境
   - 真实的交易机会

3. **策略优化** 🎯
   - 识别优势策略特征
   - 强化激进策略
   - 缩短适应期

---

## 📅 v6.0 开发路线图

### 阶段1：多样性强化 (Day 5)

**预计时间**: 2-3小时  
**优先级**: 🥇 最高

#### 1.1 提高基因变异率
```python
# 位置: prometheus/core/evolution_manager_v5.py
# 当前实现
mutation_rate = 0.1  # 10%

# v6.0 改进
base_mutation_rate = 0.2  # 20% (基础)
adaptive_mutation_rate = True  # 自适应变异

# 自适应规则
if diversity_score < 0.4:
    mutation_rate = 0.3  # 多样性低时提高到30%
elif diversity_score < 0.45:
    mutation_rate = 0.25  # 中等时25%
else:
    mutation_rate = 0.2  # 健康时20%
```

**预期效果**: 基因熵提升 50-100%

---

#### 1.2 定期注入新基因（移民机制）
```python
# 位置: prometheus/core/evolution_manager_v5.py
# 新功能: run_evolution_cycle中添加

class EvolutionManagerV5:
    def __init__(self, ...):
        self.immigration_interval = 10  # 每10轮
        self.immigrants_per_cycle = 2   # 每次2个
        
    def run_evolution_cycle(self, cycle_num: int):
        # ... 现有逻辑 ...
        
        # 6.0 新增: 定期移民
        if cycle_num % self.immigration_interval == 0:
            self._inject_immigrants()
            
    def _inject_immigrants(self):
        """注入全新基因的Agent（移民）"""
        for _ in range(self.immigrants_per_cycle):
            immigrant = self.moirai._clotho_create_v5_agents(
                count=1,
                allow_new_families=True
            )[0]
            immigrant.fitness = 1.0  # 初始适应度
            self.moirai.agents.append(immigrant)
            logger.info(f"🛬 移民到达: {immigrant.agent_id}")
```

**预期效果**: 持续注入新基因，防止基因池枯竭

---

#### 1.3 家族间强制交配（打破生殖隔离）
```python
# 位置: prometheus/core/diversity_protection.py
# 修改: force_diverse_breeding

def force_diverse_breeding(self, agents: List[AgentV5]) -> List[Tuple[AgentV5, AgentV5]]:
    """强制多样化繁殖（v6.0增强）"""
    pairs = []
    
    # 按家族分组
    families = self._group_by_family(agents)
    
    # v6.0 新增: 家族间强制交配
    if len(families) >= 2:
        # 选择最大和最小的两个家族
        sorted_families = sorted(families.items(), key=lambda x: len(x[1]))
        small_family = sorted_families[0][1]  # 最小家族
        large_family = sorted_families[-1][1]  # 最大家族
        
        # 强制跨家族交配（忽略生殖隔离）
        for small_agent in small_family[:3]:  # 最多3对
            large_agent = random.choice(large_family)
            pairs.append((small_agent, large_agent))
            logger.info(f"🧬 跨家族交配: {small_agent.lineage.family_id} × {large_agent.lineage.family_id}")
    
    return pairs
```

**预期效果**: 家族多样性提升，打破家族壁垒

---

#### 1.4 增强家族保护力度
```python
# 位置: prometheus/core/diversity_protection.py
# 修改: protect

def protect(self, agents: List[AgentV5], diversity_metrics: DiversityMetrics):
    """多样性保护（v6.0增强）"""
    
    # v6.0 调整: 降低保护阈值
    niche_threshold = 3  # 原5个 → 3个（更激进保护）
    rare_family_threshold = 5  # 原10个 → 5个
    
    # v6.0 新增: 极小家族免死金牌
    tiny_family_threshold = 2  # 家族成员≤2个，完全免死
    
    protected_ids = set()
    
    # 识别极小家族（完全保护）
    families = self._group_by_family(agents)
    for family_id, members in families.items():
        if len(members) <= tiny_family_threshold:
            for agent in members:
                protected_ids.add(agent.agent_id)
            logger.info(f"🛡️ 极小家族完全保护: {family_id} ({len(members)}个成员)")
    
    # ... 其他保护逻辑 ...
```

**预期效果**: 家族灭绝率降低，维持更多家族

---

### 阶段2：真实市场集成 (Day 6)

**预计时间**: 3-4小时  
**优先级**: 🥈 高

#### 2.1 历史K线数据加载器
```python
# 新文件: prometheus/market/historical_data.py

import ccxt
from datetime import datetime, timedelta
from typing import List, Dict

class HistoricalDataLoader:
    """历史K线数据加载器"""
    
    def __init__(self, exchange: str = 'okx'):
        self.exchange = getattr(ccxt, exchange)()
        
    def load_klines(self, 
                    symbol: str = 'BTC/USDT',
                    timeframe: str = '1h',
                    days: int = 30) -> List[Dict]:
        """
        加载历史K线数据
        
        Args:
            symbol: 交易对
            timeframe: 时间周期（1m, 5m, 15m, 1h, 4h, 1d）
            days: 天数
            
        Returns:
            List[Dict]: K线数据列表
            每个Dict包含: timestamp, open, high, low, close, volume
        """
        since = int((datetime.now() - timedelta(days=days)).timestamp() * 1000)
        klines = self.exchange.fetch_ohlcv(symbol, timeframe, since)
        
        return [{
            'timestamp': datetime.fromtimestamp(k[0] / 1000),
            'open': k[1],
            'high': k[2],
            'low': k[3],
            'close': k[4],
            'volume': k[5]
        } for k in klines]
        
    def get_price_statistics(self, klines: List[Dict]) -> Dict:
        """计算价格统计"""
        closes = [k['close'] for k in klines]
        return {
            'mean': np.mean(closes),
            'std': np.std(closes),
            'min': min(closes),
            'max': max(closes),
            'volatility': np.std(closes) / np.mean(closes)
        }
```

---

#### 2.2 真实回测框架
```python
# 新文件: test_backtest_with_real_klines.py

class HistoricalBacktest:
    """基于真实K线的回测系统"""
    
    def __init__(self, 
                 symbol: str = 'BTC/USDT',
                 timeframe: str = '1h',
                 days: int = 30,
                 evolution_interval: int = 24):  # 每24小时进化一次
        
        self.data_loader = HistoricalDataLoader()
        self.klines = self.data_loader.load_klines(symbol, timeframe, days)
        self.evolution_interval = evolution_interval
        
        # 初始化系统
        self.moirai = Moirai(...)
        self.evolution_manager = EvolutionManagerV5(...)
        self.market = SimpleOpponentMarket(
            enable_natural_volatility=False  # 使用真实价格，不需要人工波动
        )
        
    def run_backtest(self):
        """运行完整回测"""
        logger.info(f"🔄 开始回测: {len(self.klines)}根K线")
        
        evolution_cycle = 0
        
        for i, kline in enumerate(self.klines):
            # 1. 使用真实价格更新市场
            current_price = kline['close']
            
            # 2. 对手根据真实价格交易
            opponent_trades, new_price = self.market.simulate_step(
                current_price, 
                kline['timestamp']
            )
            
            # 3. Agent根据真实价格做决策（这里需要实现Agent的交易逻辑）
            # TODO: Agent实际交易模拟
            
            # 4. 定期进化
            if (i + 1) % self.evolution_interval == 0:
                evolution_cycle += 1
                logger.info(f"🧬 进化周期 {evolution_cycle} (K线 {i+1}/{len(self.klines)})")
                self.evolution_manager.run_evolution_cycle(current_price)
                
            # 5. 记录数据
            self._record_data(i, kline, current_price)
            
        logger.info("✅ 回测完成")
        self._generate_report()
```

**预期效果**: 在真实市场数据上测试Agent，获得真实表现

---

#### 2.3 Agent真实交易模拟
```python
# 修改: prometheus/core/agent_v5.py
# 新增方法: make_trading_decision

class AgentV5:
    def make_trading_decision(self, market_data: Dict) -> Optional[Dict]:
        """
        根据市场数据做出交易决策
        
        Args:
            market_data: {
                'price': current_price,
                'timestamp': datetime,
                'volume': volume,
                'volatility': volatility
            }
            
        Returns:
            Optional[Dict]: 交易决策
            {
                'action': 'buy' | 'sell' | 'hold',
                'amount': float,
                'confidence': float
            }
        """
        # 基于Agent的策略和本能做决策
        # TODO: 实现具体的决策逻辑
        pass
```

---

### 阶段3：策略优化 (Day 7)

**预计时间**: 2-3小时  
**优先级**: 🥉 中

#### 3.1 优势策略识别工具
```python
# 新文件: prometheus/analysis/strategy_analyzer.py

class StrategyAnalyzer:
    """策略分析工具"""
    
    def analyze_top_performers(self, agents: List[AgentV5], top_percent: float = 0.1):
        """分析表现最好的Agent"""
        sorted_agents = sorted(agents, key=lambda a: a.fitness, reverse=True)
        top_n = max(1, int(len(sorted_agents) * top_percent))
        top_agents = sorted_agents[:top_n]
        
        # 分析共同特征
        features = {
            'avg_genome': self._analyze_genomes(top_agents),
            'common_strategies': self._analyze_strategies(top_agents),
            'lineage_patterns': self._analyze_lineages(top_agents),
            'instinct_traits': self._analyze_instincts(top_agents)
        }
        
        return features
        
    def identify_winning_strategies(self, evolution_history: List[Dict]):
        """识别获胜策略"""
        # 分析进化历史，找出长期成功的策略模式
        pass
```

---

#### 3.2 自适应参数调整
```python
# 修改: prometheus/core/evolution_manager_v5.py

class EvolutionManagerV5:
    def __init__(self, ...):
        self.adaptive_params = True  # v6.0新增
        
    def _adjust_parameters(self, diversity_metrics: DiversityMetrics):
        """根据多样性动态调整参数（v6.0新增）"""
        
        # 调整淘汰率
        if diversity_metrics.diversity_score < 0.3:
            self.elimination_ratio = 0.2  # 降低淘汰，保护多样性
        elif diversity_metrics.diversity_score > 0.6:
            self.elimination_ratio = 0.4  # 提高淘汰，加速进化
        else:
            self.elimination_ratio = 0.3  # 标准
            
        # 调整变异率
        if diversity_metrics.gene_entropy < 0.3:
            self.mutation_rate = 0.35  # 极大提高变异
        elif diversity_metrics.gene_entropy < 0.5:
            self.mutation_rate = 0.25
        else:
            self.mutation_rate = 0.20
```

---

### 阶段4：性能优化与测试 (Day 8)

**预计时间**: 2-3小时  
**优先级**: 🏅 中低

#### 4.1 长期回测（100+ K线）
```python
# 测试1: 30天1小时K线（720根）
# 测试2: 7天15分钟K线（672根）
# 测试3: 3天5分钟K线（864根）
```

#### 4.2 性能分析
```python
# 分析指标:
- 累计收益率
- 最大回撤
- 夏普比率
- 胜率
- 种群演化质量
- 多样性维持情况
```

---

## 🎯 v6.0 核心指标目标

### 多样性指标

| 指标 | v5.2现状 | v6.0目标 | 提升幅度 |
|------|----------|----------|----------|
| 基因熵 | 0.166 | 0.500+ | **+201%** |
| 策略熵 | 4.705 | 5.000+ | +6% |
| 血统熵 | 4.493 | 5.000+ | +11% |
| 活跃家族 | 6.6个 | 10个+ | **+52%** |
| 多样性得分 | 0.413 | 0.500+ | **+21%** |

### 进化质量指标

| 指标 | v5.2现状 | v6.0目标 |
|------|----------|----------|
| 种群规模 | 53个 | 50-60个 |
| 种群稳定性 | ±2.24 | ±2.00 |
| 适应周期 | 10轮 | 5轮 |

### 回测表现指标

| 指标 | v6.0目标 |
|------|----------|
| 累计收益率 | +5%+ (30天) |
| 最大回撤 | <10% |
| 夏普比率 | >0.5 |
| 月化收益 | >1.5% |

---

## 📦 交付物清单

### 代码模块
- [ ] `prometheus/core/evolution_manager_v5.py` (增强)
- [ ] `prometheus/core/diversity_protection.py` (增强)
- [ ] `prometheus/market/historical_data.py` (新建)
- [ ] `prometheus/analysis/strategy_analyzer.py` (新建)
- [ ] `test_backtest_with_real_klines.py` (新建)

### 文档
- [ ] `V6.0_RELEASE_NOTES.md` (发布说明)
- [ ] `BACKTEST_GUIDE.md` (回测指南)
- [ ] `STRATEGY_ANALYSIS_REPORT.md` (策略分析报告)

### 测试
- [ ] 多样性强化验证测试
- [ ] 真实K线回测（3组）
- [ ] 性能压力测试

---

## ⏱️ 时间规划

### Day 5：多样性强化（2-3小时）
```
任务1: 提高变异率（30分钟）
任务2: 移民机制（45分钟）
任务3: 跨家族交配（30分钟）
任务4: 增强家族保护（30分钟）
任务5: 测试验证（30分钟）
```

### Day 6：真实市场集成（3-4小时）
```
任务1: K线加载器（1小时）
任务2: 回测框架（1.5小时）
任务3: Agent交易模拟（1小时）
任务4: 初步测试（30分钟）
```

### Day 7：策略优化（2-3小时）
```
任务1: 策略分析工具（1小时）
任务2: 自适应参数（1小时）
任务3: 优化验证（1小时）
```

### Day 8：长期测试（2-3小时）
```
任务1: 30天回测（1小时）
任务2: 性能分析（1小时）
任务3: 文档编写（1小时）
```

**总计**: 9-13小时（分4天完成）

---

## 🚨 风险与挑战

### 风险1：多样性强化可能导致性能下降
**缓解措施**: 
- 分阶段测试，及时调整参数
- 保留v5.2配置作为baseline
- 允许在性能和多样性间权衡

### 风险2：真实K线数据可能暴露Agent弱点
**缓解措施**:
- 从简单场景开始（平稳行情）
- 逐步增加难度（波动行情、趋势反转）
- 持续优化策略

### 风险3：开发时间可能超预期
**缓解措施**:
- 采用MVP方法，先实现核心功能
- 可选功能延后
- 保持灵活调整

---

## 💡 未来展望（v6.1+）

### 潜在方向

1. **对手系统v2.0**
   - 更智能的机构/散户AI
   - 学习型对手（会进化）
   - 不同类型对手组合

2. **Agent认知升级**
   - 市场情绪感知
   - 新闻事件响应
   - 多时间周期分析

3. **集体智能**
   - 家族内部协作
   - 信息共享机制
   - 群体策略

4. **实盘准备**
   - 风险控制系统
   - 仓位管理
   - 滑点和手续费建模

---

## 📊 成功标准

### v6.0 发布标准

**必须满足**:
- ✅ 基因熵 ≥ 0.500
- ✅ 活跃家族 ≥ 10个
- ✅ 多样性得分 ≥ 0.500
- ✅ 真实K线回测完成（30天）
- ✅ 所有单元测试通过

**期望满足**:
- ⭐ 累计收益率 > +5% (30天)
- ⭐ 最大回撤 < 10%
- ⭐ 种群稳定性提升
- ⭐ 策略多样性保持

---

## 🎯 总结

v6.0是Prometheus从"实验室"走向"真实市场"的关键一步：

1. **多样性强化** → 让Agent更有韧性
2. **真实市场测试** → 让Agent面对真实挑战
3. **策略优化** → 让Agent更聪明

**最终目标**: 打造一个能在真实市场中自主进化、持续盈利的Agent系统。

---

**计划制定完成时间**: 2025-12-05 19:25  
**制定人**: Prometheus v5.2 AI  
**状态**: 📋 待执行

**让我们开始v6.0的征程！** 🚀✨

